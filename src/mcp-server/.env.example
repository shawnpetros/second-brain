# Neon Postgres connection string (from Vercel dashboard → Storage → Postgres)
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# OpenAI API key (for embeddings and metadata extraction)
OPENAI_API_KEY=sk-...
